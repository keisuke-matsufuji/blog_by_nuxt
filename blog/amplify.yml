version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - cd blog
        - yarn install
    build:
      commands:
        - yarn run generate
  artifacts:
    baseDirectory: /dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
